import 'package:flutter/material.dart';

class ArPrivacyPolicy extends StatelessWidget {
  const ArPrivacyPolicy({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}